#include <stdio.h>
#include <stdlib.h>

// place is contain to code too.
int main() {
	char word[100];
	printf("Enter the word: ");
	scanf("%s",word);
	printf("%d",strlen(word));
	printf("\n");
//	printf("%d",strlen("hello world"));
	
	return 0;
}
